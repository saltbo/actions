name: GoProjectDemo

on:
  push:
  pull_request:
    
jobs:
  general-ci:
    uses: saltbo/actions/.github/workflows/go.yml@master
    with:
      project-name: xxx
      language: golang
      release-binary: true
      release-docker-image: false
    # secrets:
    #   token: ${{ secrets.TOKEN }}